const options = {
  position: 'bottom left',
  duration: 2000,
}

const $notify = {
  async success(message = '', options = options) {

  },
  async error(message = '', options = options) {

  },
  async info(message = '', options = options) {

  },
  async warning(message = '', options = options) {

  }
}
